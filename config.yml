title: c language
description: I'll be uploading all the c codes here.
template:
  repo: c-language-template
  name: c-language
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
